# tupla nomeada
import numpy as np 
from collections import namedtuple
 
# namedtuple("nomeTupla","elementos da tupla")
# tupla com os indices nomeados
 
"""
Planilha com uma lista de estudantes - Colunas : id, nota, class, name
tarefa: calclar a media de nota dos estudantes 
media = sum(todas as marcas)/total estudantes
as colunas podem estar em qualquer ordem 
nome das colunas:
ID, MARKS, CLASS and NAME

entradas:
N= numero de alunos 
nome das colunas em qlqr ordem
valores de cada coluna 

Saida:
a média das notas com duas casas decimais 
"""

# n_alunos = input()
# ordem_colunas = input().split(" ")
# Turma = namedtuple("Turma",f"{ordem_colunas[0]} {ordem_colunas[1]} {ordem_colunas[2]} {ordem_colunas[3]}")

# while n_alunos > 0:
#     ordem_colunas[0],ordem_colunas[1],ordem_colunas[2],ordem_colunas[3] = input().split(" ")
 